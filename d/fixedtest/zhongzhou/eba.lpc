// eba —— 由 nitan/d/zhongzhou/eba.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-9009,-989,1)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "漕帮");
  set("long", @LONG
这里是漕帮的总部，说是什么漕帮，其实就是几个恶霸勾
结在一起在菜场这一块地方收收保护费，帮人打打架。几个流
氓正往里屋去。
LONG );
  set("exits", ([
    "enter" : __DIR__ "4/5/0",
    "north" : __DIR__ "2/3/0",
  ]));
  set("no_fly", 1);
  setup();
}