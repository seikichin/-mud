// toyy —— 由 nitan/d/zhongzhou/toyy.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-150,-130,10)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "青石路");
  set("long", @LONG
由于离中州渐远，路上见不到多少行人，你心里不由也紧
张起来。北面可以通向中州府，西边不远便是军事重地襄阳城。
LONG );
  set("exits", ([
    "southwest" : "/d/fixedtest/xiangyang/7/4/0",
  ]));
  set("no_fly", 1);
  setup();
}