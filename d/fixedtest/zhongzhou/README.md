# zhongzhou —— nitan d/zhongzhou 固定地图映射区\r\n\r\n由 `nitan_build.aardio` 从 `nitan/d/zhongzhou/` 自动转换（/std/room/vrm_fixed 固定地图）。\r\n\r\n## 文件\r\n\r\n| 文件 | 说明 |\r\n|---|---|\r\n| `zhongzhou.lpc` | 区域档（inherit `/std/room/vrm_fixed`）|\r\n| `zhongzhou.map` | 地图：每格方向位掩码 + `[LINK]` 特殊出口 |\r\n| `zhongzhou-room.lpc` | 格房 short/long 表（改文字即可改每格 look）|\r\n| `zhongzhougate.lpc` / `zhongzhougate2.lpc` | 迷宫外入口房 / 出口房 |\r\n| `<其它>.lpc` | 不能入网格的独立房间（27 个）|\r\n\r\n## 地图\r\n\r\n- 尺寸 L,M,H = 9,11,2，非空格 = 50\r\n- 入口 = `eroad3`（格 8,3,0），出口 = `majiu`（格 3,5,0），`entry_dir = up`\r\n- 原 coor 基准 (gx,gy,gz) = (-905,-104,0)，`coor = (格索引 + 基准) * 10`\r\n- 格房虚拟路径 = `/d/fixedtest/zhongzhou/<x>/<y>/<z>`\r\n- 边：方向位 114，[LINK] 10（同格 0 / 自环 0 / 到独立房 8 / 到外部 2）\r\n\r\n## 转换约定（重要）\r\n\r\n1. 房间按原 `coor/x|y|z`（10 的倍数）直接上网格，同 coor 的多间房合并成一格（长描述按房间拼接）。\r\n2. 出口方向与 coor 增量一致（且双向互反）时才写成**方向位**；否则写成 `[LINK]`，\r\n   `[LINK]` 逐条保留原出口名与目标，**原始拓扑不变**。\r\n3. `__FILE__` 出口（无尽路）写成指向本格的 `[LINK]`，语义等价。\r\n4. 原 coor 无法上网格 / 所属格没有任何方向位（值 0 = 引擎不生成房间）/ 没有 short 设置的房间，\r\n   改为**独立房间**（同目录 `<名字>.lpc`），不参与地图。\r\n5. `short`/`long` **原样保留 nitan 的 LPC 表达式**（heredoc、`#include <ansi.h>` 的颜色宏），\r\n   所以 ANSI 颜色与原版完全一致。\r\n\r\n## 校验\r\n\r\n1. `lpcc` 逐文件真编译（`cd <mudlib根>` + `lpcc.exe ..\\config.fluffos <file>`，退出码 0 = 通过）。\r\n2. `nitan_verify.aardio` 等价性验证：用 aardio 复刻引擎的读图/建房/校验，\r\n   逐格比对「新迷宫出口集」与「原房间出口集」⇒ 见 `verify_report.txt`。\r\n3. `build_report.txt` 记录生成期的全部统计与警告。\r\n\r\n## 已知情况 / 待办\r\n\r\n- **NPC / 物品未移植**（原 objects 设置与 `/npc`、`/obj` 未处理）；\r\n  `zhongzhou-room.lpc` 里留了 `setup_npc_obj()` 钩子可按格补。\r\n- **`init()` / `add_action` / `valid_leave()` 未移植**（例如 knock/push 开门、门派限制、爬城耗气等）。\r\n  若原区域靠这类动作才能连通，静态出口图里会表现为「不可达」。\r\n- **指向其它区域的出口**（原 `/d/xxx/yyy` 形式）原样保留；若 fy2005 里没有对应对象，\r\n  该出口在运行时是悬空的（见 `build_report.txt` 的「悬空外部出口」）。\r\n- 区域尚未接入任何地图入口，属于独立测试区。\r\n