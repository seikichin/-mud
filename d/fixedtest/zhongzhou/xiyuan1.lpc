// xiyuan1 —— 由 nitan/d/zhongzhou/xiyuan1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-9060,-980,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "演出大厅");
  set("long", @LONG
这里就是大厅，台上正唱着戏呢，你可以坐下来叫上一壶
茶，好好的做一次票友。台上那位演员虽不是名角，唱起戏来
倒也是有板有眼，让人心情一畅，偶然的不快顿时烟消云散。
LONG );
  set("exits", ([
    "east" : __DIR__ "0/6/0",
    "west" : __DIR__ "huazhuang",
  ]));
  set("no_fly", 1);
  setup();
}