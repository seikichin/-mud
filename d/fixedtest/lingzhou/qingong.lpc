// qingong —— 由 nitan/d/lingzhou/qingong.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-6300,2980,0)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "寝宫");
  set("long", @LONG
这里就是西夏皇帝的寝宫，一色的白玉地板，白色的纱帐，居中是
皇帝的龙榻，榻边是一张白老虎皮做的踏脚, 墙上挂着把新月弯刀，据
说是土蕃的贡品。东西两边侍卫们值夜守更的偏殿。北面墙上有幅老鹰
扑翅的巨画。
LONG );
  set("exits", ([
    "east" : __DIR__ "4/12/1",
  ]));
  set("no_fly", 1);
  setup();
}