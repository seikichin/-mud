// yixing 出口房：宜兴·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "宜兴" NOR);
  set("long", "宜兴。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}