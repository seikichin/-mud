// clxiaowu —— 由 nitan/d/yixing/clxiaowu.c 转换（独立房间，不在固定地图网格内）
// 原 coor(269,-991,-1)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "小屋");
  set("long", @LONG
这是帮众们酗酒赌钱的的小屋，正中的方桌上杯盘狼籍，有人醉倒
在一旁，不醒人事。隔扇后面另有一张赌桌，几个帮众正在吆五喝六地
赌钱。屋内烛光摇曳，似乎没有人注意你进来。
LONG );
  set("exits", ([
    "east" : __DIR__ "clzoulang2",
  ]));
  set("no_fly", 1);
  setup();
}