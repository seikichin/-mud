// clchaifang —— 由 nitan/d/yixing/clchaifang.c 转换（独立房间，不在固定地图网格内）
// 原 coor(269,-1001,-1)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "柴房");
  set("long", " ");
  set("exits", ([
    "east" : __DIR__ "clzoulang1",
  ]));
  set("no_fly", 1);
  setup();
}