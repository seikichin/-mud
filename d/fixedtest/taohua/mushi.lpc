// mushi —— 由 nitan/d/taohua/mushi.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "墓室");
  set("long", " ");
  set("exits", ([
    "up" : __DIR__ "mudao",
  ]));
  set("no_fly", 1);
  setup();
}