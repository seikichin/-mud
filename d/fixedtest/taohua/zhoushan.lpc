// zhoushan —— 由 nitan/d/taohua/zhoushan.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "舟山");
  set("long", " ");
  set("exits", ([
    "west" : "/d/fixedtest/jiaxing/3/8/0",
  ]));
  set("no_fly", 1);
  setup();
}