// didao —— 由 nitan/d/taohua/didao.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "地道");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "xiangzhong",
  ]));
  set("no_fly", 1);
  setup();
  set("exits/enter", __DIR__"grave");
}