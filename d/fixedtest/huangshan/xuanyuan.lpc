// xuanyuan —— 由 nitan/d/huangshan/xuanyuan.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "轩辕峰");
  set("long", @LONG
这是黄山后山东的最高峰，因一路怪石嶙峋，加之前面采石峰异常
难行，所以很少人烟。据说这里便是黄帝得道飞升的所在，也不知真假
但是至今留有许多道家遗迹，可见确有人在此修道。
LONG );
  set("exits", ([
    "west" : __DIR__ "2/13/5",
  ]));
  set("no_fly", 1);
  setup();
}