// tiezhang 入口房：石阶·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "石阶" NOR);
  set("long", "石阶。往 down 可进入迷宫。\n");
  set("exits", ([
    "down" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}