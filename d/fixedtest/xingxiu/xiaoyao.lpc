// xiaoyao —— 由 nitan/d/xingxiu/xiaoyao.c 转换（独立房间，不在固定地图网格内）
// 原 coor(30,100,10)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "逍遥洞");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "shidao",
  ]));
  set("no_fly", 1);
  setup();
}