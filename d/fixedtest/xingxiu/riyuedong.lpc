// riyuedong —— 由 nitan/d/xingxiu/riyuedong.c 转换（独立房间，不在固定地图网格内）
// 原 coor(60,130,20)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "日月洞");
  set("long", " ");
  set("exits", ([
    "south" : __DIR__ "riyuedong1",
  ]));
  set("no_fly", 1);
  setup();
}