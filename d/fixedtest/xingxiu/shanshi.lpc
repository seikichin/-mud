// shanshi —— 由 nitan/d/xingxiu/shanshi.c 转换（独立房间，不在固定地图网格内）
// 原 coor(30,130,30)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "山石");
  set("long", " ");
  set("exits", ([
    "eastdown" : __DIR__ "xxhb4",
  ]));
  set("no_fly", 1);
  setup();
}