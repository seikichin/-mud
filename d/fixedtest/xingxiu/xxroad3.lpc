// xxroad3 —— 由 nitan/d/xingxiu/xxroad3.c 转换（独立房间，不在固定地图网格内）
// 原 coor(170,10,30)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "六盘山");
  set("long", " ");
  set("exits", ([
    "northdown" : "/d/fixedtest/yongdeng/2/0/0",
    "southdown" : __DIR__ "xxroad2",
    "westdown" : __DIR__ "jiayuguan",
  ]));
  set("no_fly", 1);
  setup();
}