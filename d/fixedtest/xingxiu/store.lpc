// store —— 由 nitan/d/xingxiu/store.c 转换（独立房间，不在固定地图网格内）
// 原 coor(70,90,10)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "杂货铺");
  set("long", " ");
  set("exits", ([
    "east" : __DIR__ "0/7/0",
  ]));
  set("no_fly", 1);
  setup();
}