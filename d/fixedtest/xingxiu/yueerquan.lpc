// yueerquan —— 由 nitan/d/xingxiu/yueerquan.c 转换（独立房间，不在固定地图网格内）
// 原 coor(120,30,30)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "月儿泉");
  set("long", " ");
  set("exits", ([
    "southup" : __DIR__ "3/1/2",
  ]));
  set("no_fly", 1);
  setup();
}