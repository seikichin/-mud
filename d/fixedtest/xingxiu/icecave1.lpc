// icecave1 —— 由 nitan/d/xingxiu/icecave1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(270,0,10)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "冰洞");
  set("long", " ");
  set("exits", ([
    "west" : __DIR__ "icecave2",
  ]));
  set("no_fly", 1);
  setup();
}