// jushi —— 由 nitan/d/xingxiu/jushi.c 转换（独立房间，不在固定地图网格内）
// 原 coor(70,100,20)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIB"巨石"NOR);
  set("long", " ");
  set("exits", ([
    "down" : __DIR__ "xiaolu",
  ]));
  set("no_fly", 1);
  setup();
}