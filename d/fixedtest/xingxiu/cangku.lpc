// cangku —— 由 nitan/d/xingxiu/cangku.c 转换（独立房间，不在固定地图网格内）
// 原 coor(60,100,30)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "储藏室");
  set("long", @LONG
这里原来是一个长在山壁上的天然溶洞，是星宿派老巢的储藏处。
从外面看只是一个开在石山上的普通天然石洞，然因地点隐秘而且洞中
空气十分干燥，星宿派弟子将这里改造成了一个绝佳的储藏库。
LONG );
  set("exits", ([
    "out" : __DIR__ "xxroad5",
  ]));
  set("no_fly", 1);
  setup();
}