// tianroad4 —— 由 nitan/d/xingxiu/tianroad4.c 转换（独立房间，不在固定地图网格内）
// 原 coor(230,0,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "天山山路");
  set("long", " ");
  set("exits", ([
    "westup" : __DIR__ "tianroad5",
  ]));
  set("no_fly", 1);
  setup();
}