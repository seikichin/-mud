// tygate1 —— 由 nitan/d/wudang/tygate1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-290,-300,30)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "桃园篱笆");
  set("long", " ");
  set("exits", ([
    "south" : __DIR__ "tyroad10",
  ]));
  set("no_fly", 1);
  setup();
}