// wdbl1 —— 由 nitan/d/wudang/wdbl1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-2050,-840,50)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "柏林");
  set("long", " ");
  set("exits", ([
    "east" : __DIR__ "wdbl",
  ]));
  set("no_fly", 1);
  setup();
}