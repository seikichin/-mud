// kunlun 入口房：云杉林·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "云杉林" NOR);
  set("long", "云杉林。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}