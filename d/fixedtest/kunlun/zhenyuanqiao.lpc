// zhenyuanqiao —— 由 nitan/d/kunlun/zhenyuanqiao.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-60000,5000,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "镇远桥");
  set("long", " ");
  set("exits", ([
    "southeast" : "/d/fixedtest/mingjiao/6/9/8",
    "west" : __DIR__ "shanlu",
  ]));
  set("no_fly", 1);
  setup();
}