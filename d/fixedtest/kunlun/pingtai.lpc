// pingtai —— 由 nitan/d/kunlun/pingtai.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-100000,20010,40)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW"大平台"NOR);
  set("long", " ");
  set("exits", ([
    "west" : __DIR__ "xsxiepo2",
  ]));
  set("no_fly", 1);
  setup();
}