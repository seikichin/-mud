// dongmen —— 由 nitan/d/xiakedao/dongmen.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-3090,-22080,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "洞门");
  set("long", @LONG
你的面前突然一亮，眼前赫然出现一道玉石砌成的洞门，门额
上雕有三个大字迎宾馆，两旁石粱雕龙刻凤，不知凝聚着多少名工
巧匠的心血哇！更体现出主人的富有和奢侈。
LONG );
  set("exits", ([
    "enter" : __DIR__ "yingbin",
    "west" : __DIR__ "shidong1",
  ]));
  set("no_fly", 1);
  setup();
}