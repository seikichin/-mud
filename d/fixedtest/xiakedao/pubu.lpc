// pubu —— 由 nitan/d/xiakedao/pubu.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-3000,-22060,50)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "瀑布");
  set("long", " ");
  set("exits", ([
    "northdown" : __DIR__ "road4",
  ]));
  set("no_fly", 1);
  setup();
}