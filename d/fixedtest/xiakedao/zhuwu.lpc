// zhuwu —— 由 nitan/d/xiakedao/zhuwu.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "竹屋");
  set("long", @LONG
这是一个用碗口粗大的竹子搭建的屋子，离地不足三尺。有一小
竹梯搭在门口。
LONG );
  set("exits", ([
    "east" : __DIR__ "haibin",
    "enter" : __DIR__ "zhuwu2",
  ]));
  set("no_fly", 1);
  setup();
}