// xiakedao 入口房：武场·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "武场" NOR);
  set("long", "武场。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}