// midao5 —— 由 nitan/d/xiakedao/midao5.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-3090,-25000,-20)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "秘密通道");
  set("long", @LONG
这是一个石室，隐约可以感觉到四周是冰冷而坚硬的石壁，地
上和角落都是灰尘，大概好久没人来过了。
LONG );
  set("exits", ([
    "east" : __DIR__ "midao4",
    "out" : __DIR__ "1/60/0",
  ]));
  set("no_fly", 1);
  setup();
}