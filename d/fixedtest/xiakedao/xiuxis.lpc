// xiuxis —— 由 nitan/d/xiakedao/xiuxis.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-3090,-22080,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "休息室");
  set("long", @LONG
这是一间石洞，洞内中间摆着几张大床，几张桌子，看来是
个休息的地方，室中光线柔和，使人更增睡意。
LONG );
  set("exits", ([
    "east" : __DIR__ "yingbin",
  ]));
  set("no_fly", 1);
  setup();
}