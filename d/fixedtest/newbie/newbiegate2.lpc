// newbie 出口房：村口·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "村口" NOR);
  set("long", "村口。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}