// shenghu —— 由 nitan/d/xueshan/shenghu.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-30020,-8010,10)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "圣湖");
  set("long", @LONG
眼前就是著名的圣湖，碧蓝的湖面倒影着蓝天和白云，浩淼的水面
让人心旷神怡。远处金碧辉煌的雪山寺清晰可见，再远处是几座延绵的
大雪山，其中最高大的是神山，相传山中有神仙居住。雪山中融化的雪
水都汇入湖中。
LONG );
  set("exits", ([
    "east" : __DIR__ "hubian1",
    "south" : __DIR__ "tulu3",
  ]));
  set("no_fly", 1);
  setup();
}