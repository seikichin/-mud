// kedian —— 由 nitan/d/xueshan/kedian.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-30030,-8030,10)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "客店");
  set("long", @LONG
这里是一个路边的小客店。来往的行人都在这里稍事休息。往东的
路可以到达中原。
LONG );
  set("exits", ([
    "east" : __DIR__ "tulu2",
  ]));
  set("no_fly", 1);
  setup();
}