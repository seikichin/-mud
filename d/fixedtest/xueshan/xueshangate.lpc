// xueshan 入口房：侧殿·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "侧殿" NOR);
  set("long", "侧殿。往 east 可进入迷宫。\n");
  set("exits", ([
    "east" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}