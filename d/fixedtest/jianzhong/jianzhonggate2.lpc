// jianzhong 出口房：山谷·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "山谷" NOR);
  set("long", "山谷。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}