// penglai 入口房：长生村·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "长生村" NOR);
  set("long", "长生村。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}