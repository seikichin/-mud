// rope —— 由 nitan/d/gumu/rope.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-3180,-40,100)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "绳床");
  set("long", @LONG
一条挂在侧厢中的细麻绳。
LONG );
  set("no_fly", 1);
  setup();
}