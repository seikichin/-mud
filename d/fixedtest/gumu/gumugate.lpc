// gumu 入口房：密室·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "密室" NOR);
  set("long", "密室。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}