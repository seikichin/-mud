// mingjiao 入口房：深潭·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "深潭" NOR);
  set("long", "深潭。往 down 可进入迷宫。\n");
  set("exits", ([
    "down" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}