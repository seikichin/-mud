// didao2 —— 由 nitan/d/mingjiao/didao2.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "明教地道");
  set("long", " ");
  set("exits", ([
    "out" : "/d/fixedtest/lanzhou/lanzhougate",
  ]));
  set("no_fly", 1);
  setup();
}