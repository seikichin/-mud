// shuzhai —— 由 nitan/d/songshan/shuzhai.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "书斋");
  set("long", @LONG
这里是嵩山派的书斋，靠墙是一排书架，上面整整齐齐码着一排排
书，大略看去各派武籍都有。南侧靠窗处有张书桌，桌上四宝俱备。
LONG );
  set("exits", ([
    "north" : __DIR__ "eastting",
  ]));
  set("no_fly", 1);
  setup();
}