// road5 —— 由 nitan/d/kunming/road5.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "长江北岸");
  set("long", " ");
  set("exits", ([
    "north" : __DIR__ "road4",
    "south" : __DIR__ "qiao",
  ]));
  set("no_fly", 1);
  setup();
}