// shiliang 入口房：书房·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "书房" NOR);
  set("long", "书房。往 east 可进入迷宫。\n");
  set("exits", ([
    "east" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}