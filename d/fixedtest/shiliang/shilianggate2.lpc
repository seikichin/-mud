// shiliang 出口房：侧房·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "侧房" NOR);
  set("long", "侧房。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}