// death 入口房：黄泉路·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "黄泉路" NOR);
  set("long", "黄泉路。往 down 可进入迷宫。\n");
  set("exits", ([
    "down" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}