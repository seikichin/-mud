// xuanbing —— 由 nitan/d/lingjiu/xuanbing.c 转换（独立房间，不在固定地图网格内）
// 原 coor(60,100,50)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "玄冰室");
  set("long", " ");
  set("exits", ([
    "east" : __DIR__ "6/9/5",
  ]));
  set("no_fly", 1);
  setup();
}