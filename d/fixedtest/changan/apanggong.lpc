// apanggong —— 由 nitan/d/changan/apanggong.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-10790,1920,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "阿房宫");
  set("long", " ");
  set("exits", ([
    "east" : __DIR__ "road7",
  ]));
  set("no_fly", 1);
  setup();
}