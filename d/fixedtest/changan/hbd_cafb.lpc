// hbd_cafb —— 由 nitan/d/changan/hbd_cafb.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIB"黑白道长安分坛"NOR);
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "1/3/0",
  ]));
  set("no_fly", 1);
  setup();
}