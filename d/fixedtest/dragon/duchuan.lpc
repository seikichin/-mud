// duchuan —— 由 nitan/d/dragon/duchuan.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "渡船");
  set("long", "[1;32m一叶小舟，最多也就能载七个人。一名精灵族的老艄公，手\n"
"持长竹篙，正在船尾吃力地撑着船。\n");
  set("no_fly", 1);
  setup();
}