// yingroom2 —— 由 nitan/d/dali/yingroom2.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-38000,-72010,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "泥潭小屋");
  set("long", @LONG
墙里是个院子，分为两半，左一半是实土，右一半却是水塘。内
堂前是个月洞，仍无门扉。只见当前摆了一张长桌，上面放了七盏油
灯，排成天罡北斗之形。地下乱七八糟地摆了一根根的无数竹片，那
些竹片都是长约四寸，阔约二分，似乎是计数用的算子。
LONG );
  set("exits", ([
    "out" : __DIR__ "maze2",
  ]));
  set("no_fly", 1);
  setup();
}