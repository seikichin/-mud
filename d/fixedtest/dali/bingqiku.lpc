// bingqiku —— 由 nitan/d/dali/bingqiku.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-19091,-6851,-1)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "兵器房");
  set("long", @LONG
这里是镇南王府的兵器房，四周放这几个兵器架，上面是王府家
人弟子平时练剑用的竹剑。
LONG );
  set("exits", ([
    "south" : __DIR__ "chahua1",
  ]));
  set("no_fly", 1);
  setup();
}