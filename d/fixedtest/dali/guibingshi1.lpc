// guibingshi1 —— 由 nitan/d/dali/guibingshi1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-19142,-6912,8)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "贵宾室");
  set("long", @LONG
这是一间贵宾室，这里窗明几亮，用具考究。单是窗边一架
紫竹软榻已是价值不菲。凭窗眺望，隐隐于淡烟之中可见苍山的
玉局峰，只见山势清秀，翠碧欲滴，令人心旷神怡。
LONG );
  set("exits", ([
    "out" : __DIR__ "zoulang3",
  ]));
  set("no_fly", 1);
  setup();
}