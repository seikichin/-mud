// yujia —— 由 nitan/d/dali/yujia.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-19140,-6980,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "渔家");
  set("long", "(无描述)");
  set("no_fly", 1);
  setup();
  set("exits/out", __DIR__ "hexi");
}