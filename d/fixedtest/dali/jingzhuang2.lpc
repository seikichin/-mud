// jingzhuang2 —— 由 nitan/d/dali/jingzhuang2.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-19091,-6871,19)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "第二层");
  set("long", @LONG
为四神坐像，各有表情，衣褶隆起，筋节毕现，肌肉暴起，极富
立体感。
LONG );
  set("exits", ([
    "down" : __DIR__ "jingzhuang1",
    "up" : __DIR__ "jingzhuang3",
  ]));
  set("no_fly", 1);
  setup();
}