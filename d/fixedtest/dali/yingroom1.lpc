// yingroom1 —— 由 nitan/d/dali/yingroom1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-38000,-72000,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "泥潭");
  set("long", " ");
  set("exits", ([
    "north" : __DIR__ "maze1",
  ]));
  set("no_fly", 1);
  setup();
}