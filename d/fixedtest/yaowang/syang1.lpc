// syang1 —— 由 nitan/d/yaowang/syang1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(60,60,20)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "饲养场");
  set("long", " ");
  set("exits", ([
    "south" : __DIR__ "6/6/0",
  ]));
  set("no_fly", 1);
  setup();
}