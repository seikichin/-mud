// yaowang 入口房：梅花桩一·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "梅花桩一" NOR);
  set("long", "梅花桩一。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}