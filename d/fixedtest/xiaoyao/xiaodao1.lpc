// xiaodao1 —— 由 nitan/d/xiaoyao/xiaodao1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(70,-500,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "林间小道");
  set("long", @LONG
这是一条长长的林间小道，从东边一直通到西边，一眼望去，看不
到路的尽头。两旁百花争艳。令人留连忘返。
LONG );
  set("exits", ([
    "east" : __DIR__ "qingcaop",
    "west" : __DIR__ "xiaodao2",
  ]));
  set("no_fly", 1);
  setup();
}