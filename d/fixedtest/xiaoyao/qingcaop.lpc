// qingcaop —— 由 nitan/d/xiaoyao/qingcaop.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "青草坪");
  set("long", @LONG
这里是树林中的一片比较大的空地，名曰：青草坪。因为处于密
林之中所以这里平时很少有人来，但是不知道为什么今天这里却有不
少人。你看，又一个来了。
LONG );
  set("exits", ([
    "enter" : __DIR__ "midao1",
    "south" : __DIR__ "xiaodao5",
    "west" : __DIR__ "xiaodao1",
    "east" : __DIR__ "xiaodao4",
    "north" : __DIR__ "xiaodao3",
  ]));
  set("no_fly", 1);
  setup();
}