// muwu1 —— 由 nitan/d/xiaoyao/muwu1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(80,-520,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "木屋");
  set("long", " ");
  set("exits", ([
    "north" : __DIR__ "xiaodao5",
  ]));
  set("no_fly", 1);
  setup();
}