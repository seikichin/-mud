// bagua —— 由 nitan/d/shaolin/bagua.c 转换（独立房间，不在固定地图网格内）
// 原 coor(320,0,20)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "八卦阵阵心");
  set("long", " ");
  set("exits", ([
    "up" : __DIR__ "andao3",
  ]));
  set("no_fly", 1);
  setup();
}