// maowu —— 由 nitan/d/shaolin/maowu.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "茅屋");
  set("long", @LONG
这是一件不大的茅屋，残破不全。向上望去可以看到点点阳光洒
降下来，难以想象这里下雨的日子会是怎样一番情景。
LONG );
  set("exits", ([
    "west" : "/d/fixedtest/kaifeng/2/18/0",
  ]));
  set("no_fly", 1);
  setup();
}