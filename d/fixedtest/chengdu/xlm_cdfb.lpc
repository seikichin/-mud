// xlm_cdfb —— 由 nitan/d/chengdu/xlm_cdfb.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "修罗门分坛");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "qingyanggong",
  ]));
  set("no_fly", 1);
  setup();
}