// qingyanggong —— 由 nitan/d/chengdu/qingyanggong.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-15250,-1850,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "青羊宫");
  set("long", " ");
  set("exits", ([
    "northeast" : __DIR__ "0/2/0",
  ]));
  set("no_fly", 1);
  setup();
}