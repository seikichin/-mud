// minju —— 由 nitan/d/chengdu/minju.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "废弃的民居");
  set("long", @LONG
这里是一幢破烂的民居，看样子已经被废弃很常一段日子
了，大院内四处残戈断壁，杂草丛生。院子的一个阴暗角落里
还立着一个已快腐烂的书架。
LONG );
  set("exits", ([
    "out" : __DIR__ "4/2/0",
  ]));
  set("no_fly", 1);
  setup();
}