// chengdu 出口房：府河桥·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "府河桥" NOR);
  set("long", "府河桥。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}