// hengshan 出口房：大字岭·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "大字岭" NOR);
  set("long", "大字岭。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}