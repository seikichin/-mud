// ============================================================
//  northft-room.lpc  ——  nitan d/northft 映射区（10x7x3）房间模板
//  由 nitan_build.aardio 自动生成，可自由修改文字
//
//  数据表 maze_cell_desc 的键 = 格索引 ""x,y,z""（从 0 开始，与 northft.map 一致）
//  值 = ({ 短描述, 长描述 })
//  改这里的文字即可直接改变对应格子的 look 结果。
//
//  原 coor 与格索引的换算： coor = (格索引 + (0,0,0)) * 10
//  一格多间房时：短描述取主房，长描述按房间拼接（源房名见行末注释）
//
//  short/long 保留 nitan 原文的 LPC 表达式（heredoc / 颜色宏），
//  因此 ANSI 颜色（HIW / HIY / NOR 等）与原文完全一致。
// ============================================================

inherit ROOM;
#include <ansi.h>

void create() {}

// 格索引 -> ({ short, long })
mapping maze_cell_desc = ([
  "0,0,0" : ({ "青石小路", @LONG
这是奉天城北门外一条青石铺成的小路，南面就是奉天城的
北门了。路边稀疏的长着一些荒草，路上满是沙砾，阵阵北风呼
啸而过，越发给人一种凄凉的感觉。
LONG  }),  // coor(0,0,0)  ftn1
  "8,0,0" : ({ "山路", " \n" }),  // coor(80,0,0)  ftn12
  "9,0,0" : ({ "小径", @LONG
这是一条窄窄的上山小路，两旁怪石嶙峋，中间的路倒是很
平坦，看样子是被人专门开掘出来的。向东望去是一大片乱石岗。
LONG  }),  // coor(90,0,0)  ftn13
  "0,1,0" : ({ "青石小路", @LONG
这是奉天城北门外一条青石铺成的小路，南面就是奉天城的
北门了。路边稀疏的长着一些荒草，路上满是沙砾，阵阵北风呼
啸而过，越发给人一种凄凉的感觉。
LONG  }),  // coor(0,10,0)  ftn2
  "0,2,0" : ({ "青石小路", @LONG
这是奉天城北门外一条青石铺成的小路，南面就是奉天城的
北门了。路边稀疏的长着一些荒草，路上满是沙砾，阵阵北风呼
啸而过，越发给人一种凄凉的感觉。
LONG  }),  // coor(0,20,0)  ftn3
  "0,3,0" : ({ "青石小路", @LONG
这是奉天城北门外一条青石铺成的小路，南面就是奉天城的
北门了。路边稀疏的长着一些荒草，路上满是沙砾，阵阵北风呼
啸而过，越发给人一种凄凉的感觉。
LONG  }),  // coor(0,30,0)  ftn4
  "0,4,0" : ({ "青石小路", @LONG
这是奉天城北门外一条青石铺成的小路，南面就是奉天城的
北门了。路边稀疏的长着一些荒草，路上满是沙砾，阵阵北风呼
啸而过，越发给人一种凄凉的感觉。
LONG  }),  // coor(0,40,0)  ftn5
  "0,5,0" : ({ "青石小路", @LONG
这是奉天城北门外一条青石铺成的小路，南面就是奉天城的
北门了。路边稀疏的长着一些荒草，路上满是沙砾，阵阵北风呼
啸而过，越发给人一种凄凉的感觉。
LONG  }),  // coor(0,50,0)  ftn6
  "1,6,0" : ({ "山路", @LONG
这是一条窄窄的上山小路，两旁怪石嶙峋，中间的路倒是很
平坦，看样子是被人专门开掘出来的。
LONG  }),  // coor(10,60,0)  ftn7
  "2,6,1" : ({ "山路", @LONG
这是一条窄窄的上山小路，两旁怪石嶙峋，中间的路倒是很
平坦，看样子是被人专门开掘出来的。
LONG  }),  // coor(20,60,10)  ftn8
  "3,6,2" : ({ "山路", @LONG
这是一条窄窄的上山小路，两旁怪石嶙峋，中间的路倒是很
平坦，看样子是被人专门开掘出来的。
LONG  }),  // coor(30,60,20)  ftn9
  "4,6,2" : ({ "山路", @LONG
这是一条窄窄的上山小路，两旁怪石嶙峋，中间的路倒是很
平坦，看样子是被人专门开掘出来的。
LONG  }),  // coor(40,60,20)  ftn10
  "5,6,2" : ({ "山路", " \n" }),  // coor(50,60,20)  ftn11
]);

void setup_room_desc(object room, int length, int width, int height, int x, int y, int z) {
  string key;
  mixed info;

  key = sprintf("%d,%d,%d", x, y, z);
  info = maze_cell_desc[key];

  if (arrayp(info) && sizeof(info) >= 2) {
    room->set("short", info[0]);
    room->set("long", info[1]);
  } else {
    // 表里没有的格子（地图新增格）的兜底描述
    room->set("short", HIB "街巷" NOR);
    room->set("long", "空旷的街巷，看不出有什么特别之处。\n");
  }

  room->set("map_x", x);
  room->set("map_y", y);
  room->set("map_z", z);

  room->setup();
}

// 预留：按坐标放怪物 / 物品
void setup_npc_obj(object room, int length, int width, int height, int x, int y, int z) {
  return;
}

int setup_entry_room() { return 1; }
int setup_exit_room() { return 1; }