// jiugong7 —— 由 nitan/d/guiyun/jiugong7.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "九宫桃花阵");
  set("long", "(无描述)");
  set("exits", ([
    "east" : __DIR__ "jiugong8",
    "north" : __DIR__ "jiugong4",
  ]));
  set("no_fly", 1);
  setup();
}