// wuchang 入口房：四季美·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "四季美" NOR);
  set("long", "四季美。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}