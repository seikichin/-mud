// heizhao 出口房：西厢房·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "西厢房" NOR);
  set("long", "西厢房。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}