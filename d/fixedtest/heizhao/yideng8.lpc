// yideng8 —— 由 nitan/d/heizhao/yideng8.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-38000,-80010,500)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "山梁");
  set("long", " ");
  set("exits", ([
    "down" : __DIR__ "yideng7",
  ]));
  set("no_fly", 1);
  setup();
}