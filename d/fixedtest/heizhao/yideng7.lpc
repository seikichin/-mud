// yideng7 —— 由 nitan/d/heizhao/yideng7.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-38000,-80000,300)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "缺口");
  set("long", " ");
  set("exits", ([
    "down" : __DIR__ "yideng5",
  ]));
  set("no_fly", 1);
  setup();
}