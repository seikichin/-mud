// baituo 出口房：草坪·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "草坪" NOR);
  set("long", "草坪。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}