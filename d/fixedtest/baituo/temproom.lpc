// temproom —— 由 nitan/d/baituo/temproom.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "小路");
  set("long", @LONG
通往蛇场内部的道路。
LONG );
  set("no_fly", 1);
  setup();
}