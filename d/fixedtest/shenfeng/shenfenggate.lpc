// shenfeng 入口房：荒道·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "荒道" NOR);
  set("long", "荒道。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}