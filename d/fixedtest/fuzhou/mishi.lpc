// mishi —— 由 nitan/d/fuzhou/mishi.c 转换（独立房间，不在固定地图网格内）
// 原 coor(1820,-6312,0)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "密室");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "midao",
  ]));
  set("no_fly", 1);
  setup();
}