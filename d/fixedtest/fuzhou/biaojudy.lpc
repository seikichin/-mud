// biaojudy —— 由 nitan/d/fuzhou/biaojudy.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-101,-6171,-1)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "大院");
  set("long", @LONG
大院里显得有些凌乱，趟子手和镖师们进进出出，似乎都在满地里
忙碌着什么。一棵很大的榕树栽在院子中央，给火辣辣的福州带来一点
凉爽。
LONG );
  set("exits", ([
    "west" : __DIR__ "biaojucyf",
    "north" : __DIR__ "biaojuzt",
    "east" : __DIR__ "biaojuhc",
    "out" : __DIR__ "0/6/0",
  ]));
  set("no_fly", 1);
  setup();
}