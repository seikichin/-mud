// zahuopu —— 由 nitan/d/fuzhou/zahuopu.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-110,-6200,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "杂货铺");
  set("long", @LONG
这里是一家专卖女人用的东西的小铺子。
LONG );
  set("exits", ([
    "east" : __DIR__ "0/3/0",
  ]));
  set("no_fly", 1);
  setup();
}