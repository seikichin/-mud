// zhulin1 —— 由 nitan/d/kaifeng/zhulin1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-5021,2139,9)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "小茅屋");
  set("long", @LONG
你走进竹林中的小茅屋，打量了一下，在茅屋仅是用茅草覆顶，
四边的墙壁是用一根根大毛竹打入地下而成的，屋中的桌椅也都是翠
竹所制，桌上放的茶壶是用整个竹根雕成的。
LONG );
  set("exits", ([
    "out" : __DIR__ "8/5/1",
  ]));
  set("no_fly", 1);
  setup();
}