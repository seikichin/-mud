// chuantang —— 由 nitan/d/yanziwu/chuantang.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "穿堂");
  set("long", @LONG
不知是什么能工巧匠设计了这条穿堂，九曲回折，雕梁画栋。再
望南就是后院了，向北则是一座大厅堂。
LONG );
  set("exits", ([
    "north" : __DIR__ "cuixia",
    "south" : __DIR__ "lixiang",
  ]));
  set("no_fly", 1);
  setup();
}