// qinfang2 —— 由 nitan/d/yanziwu/qinfang2.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "沁芳阁二层");
  set("long", @LONG
这儿窗户半闭，嗅着透过来的阵阵清香，你觉得有些困倦。一张
大床上淡绿色的被褥发出淡淡的香气，睡了吧，你不由的想到。
LONG );
  set("exits", ([
    "down" : __DIR__ "qinfang1",
  ]));
  set("no_fly", 1);
  setup();
}