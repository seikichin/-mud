// huizhen —— 由 nitan/d/yanziwu/huizhen.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "汇珍园");
  set("long", @LONG
这是燕子坞中最大的花园，遍植江南的奇花异草，香气浓郁。彩
蝶在花间翩跹飞舞，你不由的痴了，有点乐而忘返。
LONG );
  set("exits", ([
    "west" : __DIR__ "changlang",
  ]));
  set("no_fly", 1);
  setup();
}