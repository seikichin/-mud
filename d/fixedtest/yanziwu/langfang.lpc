// langfang —— 由 nitan/d/yanziwu/langfang.c 转换（独立房间，不在固定地图网格内）
// 原 coor(1190,-1270,-2)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "琅缳玉洞");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "dong8",
  ]));
  set("no_fly", 1);
  setup();
}