// tingyu —— 由 nitan/d/yanziwu/tingyu.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "听雨居");
  set("long", " ");
  set("exits", ([
    "south" : __DIR__ "xiaoqiao",
  ]));
  set("no_fly", 1);
  setup();
}