// kuxiu2 —— 由 nitan/d/yanziwu/kuxiu2.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "苦修场");
  set("long", @LONG
这里的场地有些破落，虽然经常打扫，但是场地仍然看得出不少
痕迹。希望出人头地的慕容家弟子在武功有了一定的修为以后往往到
这里来作进一步的修行。
LONG );
  set("exits", ([
    "west" : __DIR__ "kuxiu",
  ]));
  set("no_fly", 1);
  setup();
}