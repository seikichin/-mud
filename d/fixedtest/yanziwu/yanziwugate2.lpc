// yanziwu 出口房：乱云亭·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "乱云亭" NOR);
  set("long", "乱云亭。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}