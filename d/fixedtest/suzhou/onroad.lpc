// onroad —— 由 nitan/d/suzhou/onroad.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "路上");
  set("long", "(无描述)");
  set("no_fly", 1);
  setup();
}