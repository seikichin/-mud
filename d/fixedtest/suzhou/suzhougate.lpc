// suzhou 入口房：麻石大道·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "麻石大道" NOR);
  set("long", "麻石大道。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}