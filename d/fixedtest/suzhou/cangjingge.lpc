// cangjingge —— 由 nitan/d/suzhou/cangjingge.c 转换（独立房间，不在固定地图网格内）
// 原 coor(1119,-1151,29)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "藏经阁");
  set("long", " ");
  set("exits", ([
    "south" : __DIR__ "dxbdian",
  ]));
  set("no_fly", 1);
  setup();
}