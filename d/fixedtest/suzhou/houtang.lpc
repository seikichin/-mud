// houtang —— 由 nitan/d/suzhou/houtang.c 转换（独立房间，不在固定地图网格内）
// 原 coor(1089,-1051,-1)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "后堂");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "6/12/0",
  ]));
  set("no_fly", 1);
  setup();
}