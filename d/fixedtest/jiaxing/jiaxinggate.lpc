// jiaxing 入口房：山路·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "山路" NOR);
  set("long", "山路。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}