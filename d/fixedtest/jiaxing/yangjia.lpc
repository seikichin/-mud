// yangjia —— 由 nitan/d/jiaxing/yangjia.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "杨家小屋");
  set("long", " ");
  set("exits", ([
    "south" : "/d/fixedtest/jiaxing/qianyuan",
    "north" : "/d/fixedtest/jiaxing/houyuan",
  ]));
  set("no_fly", 1);
  setup();
}