// qianyuan —— 由 nitan/d/jiaxing/qianyuan.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", GRN "前院" NOR);
  set("long", " ");
  set("exits", ([
    "south" : __DIR__ "9/4/0",
    "north" : __DIR__ "yangjia",
  ]));
  set("no_fly", 1);
  setup();
}