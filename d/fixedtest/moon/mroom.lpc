// mroom —— 由 nitan/d/moon/mroom.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-50,1120,40)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "密室");
  set("long", @LONG
一间小小的密室，一尘不染，看来经常有人来打扫，
可是现在这里除了一个书柜以外，别的什么都没有。
LONG );
  set("no_fly", 1);
  setup();
  set("exits/south", __DIR__"nroom");
}
// [nitan-auto] 由原档补回的额外函数（恢复离开房间的行为）