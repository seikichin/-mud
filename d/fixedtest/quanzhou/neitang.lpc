// neitang —— 由 nitan/d/quanzhou/neitang.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-921,-7681,-1)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "内堂");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "2/10/1",
  ]));
  set("no_fly", 1);
  setup();
}