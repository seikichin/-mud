// quanzhou 入口房：山路·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "山路" NOR);
  set("long", "山路。往 north 可进入迷宫。\n");
  set("exits", ([
    "north" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}