// guanwai 出口房：老龙头·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "老龙头" NOR);
  set("long", "老龙头。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}