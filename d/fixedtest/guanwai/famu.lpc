// famu —— 由 nitan/d/guanwai/famu.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "伐木场");
  set("long", " ");
  set("exits", ([
    "southup" : __DIR__ "luming",
  ]));
  set("no_fly", 1);
  setup();
}