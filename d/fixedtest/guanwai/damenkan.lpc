// damenkan —— 由 nitan/d/guanwai/damenkan.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "大门坎子");
  set("long", " ");
  set("exits", ([
    "southeast" : __DIR__ "ermenkan",
  ]));
  set("no_fly", 1);
  setup();
}