// hanshui1 —— 由 nitan/d/nanyang/hanshui1.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "汉水南岸");
  set("long", " ");
  set("exits", ([
    "south" : __DIR__ "yidao2",
  ]));
  set("no_fly", 1);
  setup();
}