// tianlongsi 出口房：牟尼堂·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "牟尼堂" NOR);
  set("long", "牟尼堂。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}