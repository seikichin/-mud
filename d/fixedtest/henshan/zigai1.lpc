// zigai1 —— 由 nitan/d/henshan/zigai1.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "水莲洞内");
  set("long", @LONG
这里一片漆黑，什么都看不见，只听见水滴的声音。
LONG );
  set("exits", ([
    "east" : __DIR__ "inhole2",
  ]));
  set("no_fly", 1);
  setup();
}