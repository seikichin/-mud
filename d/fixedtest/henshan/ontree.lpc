// ontree —— 由 nitan/d/henshan/ontree.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "古树上");
  set("long", " ");
  set("exits", ([
    "down" : __DIR__ "xuanyadi",
    "up" : __DIR__ "ontree2",
  ]));
  set("no_fly", 1);
  setup();
}