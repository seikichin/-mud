// inhole —— 由 nitan/d/henshan/inhole.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "水帘洞外洞");
  set("long", " ");
  set("exits", ([
    "enter" : __DIR__ "inhole1",
  ]));
  set("no_fly", 1);
  setup();
}