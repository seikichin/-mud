// furong —— 由 nitan/d/henshan/furong.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "芙蓉洞外");
  set("long", @LONG
只见此处有一个小洞，洞口狭小，只能容一人通过。洞中却射出
一缕阳光。
LONG );
  set("exits", ([
    "enter" : __DIR__ "furong1",
  ]));
  set("no_fly", 1);
  setup();
}