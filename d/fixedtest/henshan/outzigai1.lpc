// outzigai1 —— 由 nitan/d/henshan/outzigai1.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "突石上");
  set("long", " ");
  set("exits", ([
    "down" : __DIR__ "outzigai",
  ]));
  set("no_fly", 1);
  setup();
  set("exits/enter", __DIR__"zigaihole");
}