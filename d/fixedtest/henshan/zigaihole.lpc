// zigaihole —— 由 nitan/d/henshan/zigaihole.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "紫盖仙洞");
  set("long", " ");
  set("exits", ([
    "north" : __DIR__ "xuanya",
  ]));
  set("no_fly", 1);
  setup();
}