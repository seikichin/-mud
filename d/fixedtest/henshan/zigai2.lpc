// zigai2 —— 由 nitan/d/henshan/zigai2.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "水莲洞内");
  set("long", @LONG
这里四周空旷，比起外面来却没有那么潮湿。
LONG );
  set("exits", ([
    "south" : __DIR__ "inhole2",
  ]));
  set("no_fly", 1);
  setup();
  set("exits/north", __DIR__"zigai3");
}