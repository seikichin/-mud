// furong1 —— 由 nitan/d/henshan/furong1.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "芙蓉洞");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "furong",
  ]));
  set("no_fly", 1);
  setup();
}