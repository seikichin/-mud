// yudong —— 由 nitan/d/lingxiao/yudong.c 转换（独立房间，不在固定地图网格内）
// 原 coor(60,50,50)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "玉洞");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "wave",
  ]));
  set("no_fly", 1);
  setup();
}