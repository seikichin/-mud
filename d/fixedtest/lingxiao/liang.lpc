// liang —— 由 nitan/d/lingxiao/liang.c 转换（独立房间，不在固定地图网格内）
// 原 coor(10,100,60)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "居室");
  set("long", @LONG
这是梁自进的房间，他生性单纯，与廖自励正好相反。他从
小就在凌霄城中练功习武，连大雪山也未下过一步，所以他的武
功在五代弟子中，是仅次于白自在的。他的房间朴素自然，仅有
一床一桌。除此之外，别无他物。 
LONG );
  set("exits", ([
    "north" : __DIR__ "2/10/6",
  ]));
  set("no_fly", 1);
  setup();
}