// duzunju —— 由 nitan/d/taishan/duzunju.c 转换（独立房间，不在固定地图网格内）
// 原 coor(410,650,120)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "独尊居");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "1/31/22",
  ]));
  set("no_fly", 1);
  setup();
}