// taishan 入口房：东天门·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "东天门" NOR);
  set("long", "东天门。往 north 可进入迷宫。\n");
  set("exits", ([
    "north" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}