// xuedao 出口房：雪山山路·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "雪山山路" NOR);
  set("long", "雪山山路。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}