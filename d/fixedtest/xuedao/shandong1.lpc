// shandong1 —— 由 nitan/d/xuedao/shandong1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(10,0,30)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "山洞");
  set("long", @LONG
空空荡荡一个山洞，倒也还算干净，尽头摆了一张石床。
LONG );
  set("exits", ([
    "out" : __DIR__ "0/0/3",
  ]));
  set("no_fly", 1);
  setup();
}