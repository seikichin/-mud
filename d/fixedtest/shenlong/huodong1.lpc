// huodong1 —— 由 nitan/d/shenlong/huodong1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(730,170,0)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIR "火焰洞" NOR);
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "72/17/0",
  ]));
  set("no_fly", 1);
  setup();
}