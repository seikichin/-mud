// qingcheng —— 由 nitan/d/yongdeng/qingcheng.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-10000,800,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "青城");
  set("long", @LONG
青城位于靖远以南三百二十里，西南八十里就是兰州，名为青城，
其实只是个小镇，因为地处北去西夏的要冲，许多往来西夏和大宋的商
旅都必须经过这儿，人来人往很是热闹。
LONG );
  set("exits", ([
    "east" : "/d/fixedtest/huanghe/hetao",
    "north" : __DIR__ "6/4/0",
  ]));
  set("no_fly", 1);
  setup();
}