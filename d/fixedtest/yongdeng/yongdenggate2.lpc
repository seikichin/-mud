// yongdeng 出口房：永登·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "永登" NOR);
  set("long", "永登。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}