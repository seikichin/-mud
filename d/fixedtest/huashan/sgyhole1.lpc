// sgyhole1 —— 由 nitan/d/huashan/sgyhole1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(70,0,130)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "山洞");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "6/0/13",
  ]));
  set("no_fly", 1);
  setup();
  set("exits/enter", "/d/fixedtest/huashan/16/2/11");
}