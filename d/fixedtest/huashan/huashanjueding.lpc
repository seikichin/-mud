// huashanjueding —— 由 nitan/d/huashan/huashanjueding.c 转换（独立房间，不在固定地图网格内）
// 原 coor(50,100,90)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "华山绝顶");
  set("long", " ");
  set("exits", ([
    "southdown" : __DIR__ "5/9/8",
  ]));
  set("no_fly", 1);
  setup();
}