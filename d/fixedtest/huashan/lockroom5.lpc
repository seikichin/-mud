// lockroom5 —— 由 nitan/d/huashan/lockroom5.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "石室");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "s",
  ]));
  set("no_fly", 1);
  setup();
}