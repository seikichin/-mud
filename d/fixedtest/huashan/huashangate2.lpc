// huashan 出口房：松树林·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "松树林" NOR);
  set("long", "松树林。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}