// huashan 入口房：小木屋前·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "小木屋前" NOR);
  set("long", "小木屋前。往 down 可进入迷宫。\n");
  set("exits", ([
    "down" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}