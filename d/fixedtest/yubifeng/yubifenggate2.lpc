// yubifeng 出口房：玉笔峰·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "玉笔峰" NOR);
  set("long", "玉笔峰。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}