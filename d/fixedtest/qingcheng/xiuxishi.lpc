// xiuxishi —— 由 nitan/d/qingcheng/xiuxishi.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-8091,-840,90)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "厢房");
  set("long", @LONG
这是间整洁的厢房，明净透亮的窗户用厚厚的窗纱遮了大半，光线
很昏暗。房里别无他物，只有中间放着一张收拾得舒舒服服的大床，看
着就让人想睡觉。看来这里是青城休息的地方。
LONG );
  set("no_fly", 1);
  setup();
}