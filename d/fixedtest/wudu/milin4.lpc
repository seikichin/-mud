// milin4 —— 由 nitan/d/wudu/milin4.c 转换（独立房间，不在固定地图网格内）
// 原 coor(160,40,30)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "黑森林");
  set("long", @LONG
这里就是有名的黑森林，四周均是茂密的丛林，巨大的古木遮天
蔽日，头顶看不到一片天空。脚下不时传来轻微的声响，似乎有什么
东西从草丛中游过，远处偶尔传来一阵野兽的嚎叫声。
LONG );
  set("exits", ([
    "enter" : __DIR__ "langwo",
    "north" : __DIR__ "milin3",
    "west" : __DIR__ "milin3",
    "south" : __DIR__ "milin5",
  ]));
  set("no_fly", 1);
  setup();
}