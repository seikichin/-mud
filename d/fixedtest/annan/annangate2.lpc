// annan 出口房：丘陵·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "丘陵" NOR);
  set("long", "丘陵。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}