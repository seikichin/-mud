// hcaxiuxishi —— 由 nitan/d/emei/hcaxiuxishi.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "休息室");
  set("long", @LONG
这里是峨嵋华藏庵的休息室。窗帘拉下来，房里整整齐齐放了很
多木床。有几个夜晚守庵的小师太正在睡觉。休息室外就是禅房。
LONG );
  set("exits", ([
    "north" : __DIR__ "hcachanfang",
  ]));
  set("no_fly", 1);
  setup();
}