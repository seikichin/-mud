// cangjinglou —— 由 nitan/d/emei/cangjinglou.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "藏经楼");
  set("long", @LONG
这里是报国寺藏经楼。阁楼上密密麻麻排了好多书架，架上摆满
了经书。有两个身穿白衣的小师太正在一角静静地读着经书。
LONG );
  set("exits", ([
    "south" : __DIR__ "dxdian",
  ]));
  set("no_fly", 1);
  setup();
}