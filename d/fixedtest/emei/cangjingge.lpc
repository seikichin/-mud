// cangjingge —— 由 nitan/d/emei/cangjingge.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "藏经阁");
  set("long", @LONG
小阁楼上便是峨嵋派的藏经阁了。这里都是密密麻麻，高及顶棚
的书架。窗口下有张大桌子，桌上放了几本佛经。有个小师太正在向
静道师太借书。
LONG );
  set("exits", ([
    "down" : __DIR__ "chuwujian",
  ]));
  set("no_fly", 1);
  setup();
}