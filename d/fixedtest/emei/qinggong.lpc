// qinggong —— 由 nitan/d/emei/qinggong.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "峨嵋寝宫");
  set("long", @LONG
这是峨嵋山金顶华藏庵外的一片小树林峨嵋寝宫。
LONG );
  set("exits", ([
    "south" : __DIR__ "xiaoshulin4",
  ]));
  set("no_fly", 1);
  setup();
}