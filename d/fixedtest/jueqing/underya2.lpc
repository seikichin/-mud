// underya2 —— 由 nitan/d/jueqing/underya2.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "寒潭尽头");
  set("long", " ");
  set("exits", ([
    "enter" : __DIR__ "house",
  ]));
  set("no_fly", 1);
  setup();
}