// house —— 由 nitan/d/jueqing/house.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "小屋");
  set("long", @LONG
这是绝情谷底的一间小茅屋，这里似乎很久没有来过了。
LONG );
  set("exits", ([
    "out" : __DIR__ "underya2",
  ]));
  set("no_fly", 1);
  setup();
}