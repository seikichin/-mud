// baihuagu 出口房：周伯通居·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "周伯通居" NOR);
  set("long", "周伯通居。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}