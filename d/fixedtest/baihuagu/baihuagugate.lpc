// baihuagu 入口房：山谷中·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "山谷中" NOR);
  set("long", "山谷中。往 down 可进入迷宫。\n");
  set("exits", ([
    "down" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}