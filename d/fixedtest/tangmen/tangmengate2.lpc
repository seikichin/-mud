// tangmen 出口房：唐氏钱庄·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "唐氏钱庄" NOR);
  set("long", "唐氏钱庄。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}