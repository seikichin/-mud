// tangmen 入口房：地道·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "地道" NOR);
  set("long", "地道。往 north 可进入迷宫。\n");
  set("exits", ([
    "north" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}