// jingzhou 出口房：山冈·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "山冈" NOR);
  set("long", "山冈。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}