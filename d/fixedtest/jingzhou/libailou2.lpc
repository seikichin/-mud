// libailou2 —— 由 nitan/d/jingzhou/libailou2.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "李白楼二楼");
  set("long", " ");
  set("exits", ([
    "down" : __DIR__ "libailou",
  ]));
  set("no_fly", 1);
  setup();
}