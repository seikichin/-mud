// sec3 —— 由 nitan/d/jingzhou/sec3.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "坟墓内部");
  set("long", " ");
  set("exits", ([
    "north" : __DIR__ "sec2",
  ]));
  set("no_fly", 1);
  setup();
}