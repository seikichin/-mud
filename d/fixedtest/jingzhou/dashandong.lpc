// dashandong —— 由 nitan/d/jingzhou/dashandong.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-7131,-2181,19)  coor 不是 10 的倍数
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "大山洞");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "tulu4",
  ]));
  set("no_fly", 1);
  setup();
}