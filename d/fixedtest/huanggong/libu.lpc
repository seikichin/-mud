// libu —— 由 nitan/d/huanggong/libu.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-2790,7700,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "吏部公署");
  set("long", @LONG
这里是六部之首--吏部官员候朝的朝房.
LONG );
  set("exits", ([
    "south" : __DIR__ "youmen",
  ]));
  set("no_fly", 1);
  setup();
}