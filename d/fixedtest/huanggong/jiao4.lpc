// jiao4 —— 由 nitan/d/huanggong/jiao4.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-2820,7700,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "城角");
  set("long", @LONG
这里是内城的一角, 头上的天空只能见方方的一角. 上面有座漂亮
的角楼.
LONG );
  set("exits", ([
    "north" : __DIR__ "0/0/0",
    "east" : __DIR__ "dao10",
    "up" : __DIR__ "lou4",
  ]));
  set("no_fly", 1);
  setup();
}