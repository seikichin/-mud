// bingbu —— 由 nitan/d/huanggong/bingbu.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-2810,7700,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "兵部公署");
  set("long", @LONG
这里是六部之四--兵部官员候朝的朝房.
LONG );
  set("exits", ([
    "south" : __DIR__ "zuomen",
  ]));
  set("no_fly", 1);
  setup();
}