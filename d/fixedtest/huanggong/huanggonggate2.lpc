// huanggong 出口房：景山·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "景山" NOR);
  set("long", "景山。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}