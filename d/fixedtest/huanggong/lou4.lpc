// lou4 —— 由 nitan/d/huanggong/lou4.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-2820,7700,10)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "角楼");
  set("long", @LONG
角楼中央是一个三开间的方亭楼, 四出抱厦. 最顶部十字脊镀金宝
顶以下, 计有三层檐, 九梁, 十八柱, 七十二条脊, 上下重叠, 纵横交
错. 底座是白玉石台基周围饰以白玉栏杆. 它紧扼内城一角, 能控制四
下, 内城四角各有一座这样的角楼, 若有风吹草动, 都可一清二楚.
LONG );
  set("exits", ([
    "down" : __DIR__ "jiao4",
  ]));
  set("no_fly", 1);
  setup();
}