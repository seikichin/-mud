// dianwai —— 由 nitan/d/huanggong/dianwai.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-2800,7690,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "午门广场");
  set("long", @LONG
这里是皇帝廷杖官吏的地方, 也是大将得胜还朝后举行献俘礼仪的
场所. 每年十月初一日, 在这里向全国颁发来年历书, 时称“颁朔”.
在内金水河上的五座汉白玉雕桥, 造型秀丽, 同雄伟壮观的午门城楼和
金碧辉煌的太和门相映, 引人注目. 
LONG );
  set("exits", ([
    "south" : "/d/fixedtest/beijing/4/7/0",
    "north" : __DIR__ "wumen",
    "east" : __DIR__ "youmen",
    "west" : __DIR__ "zuomen",
  ]));
  set("no_fly", 1);
  setup();
}