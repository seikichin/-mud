// underfz —— 由 nitan/d/gaibang/underfz.c 转换（独立房间，不在固定地图网格内）
// 原 coor(1840,-6320,-20)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "大榕树下");
  set("long", @LONG
这是福州大榕树下的小洞。里面破破烂烂的，丢满了各种杂物，还
有丐帮弟子收集的破铜烂铁等。从这里通往丐帮的秘密通道。
LONG );
  set("exits", ([
    "northwest" : __DIR__ "fzandao2",
    "out" : "/d/fixedtest/fuzhou/2/3/0",
  ]));
  set("no_fly", 1);
  setup();
}