// underwx —— 由 nitan/d/gaibang/underwx.c 转换（独立房间，不在固定地图网格内）
// 原 coor(400,-740,-20)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "草堆下面");
  set("long", @LONG
这是无锡城外杏子林丐帮大义分舵草堆的洞里。四周丢满了各种杂
物，还有丐帮弟子收集的破铜烂铁等。从这里通往丐帮的秘密通道。
LONG );
  set("exits", ([
    "north" : __DIR__ "wxandao2",
    "out" : "/d/fixedtest/wuxi/9/10/0",
  ]));
  set("no_fly", 1);
  setup();
}