// minju1 —— 由 nitan/d/hanzhong/minju1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-12260,820,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "民居");
  set("long", " ");
  set("exits", ([
    "east" : __DIR__ "0/4/0",
  ]));
  set("no_fly", 1);
  setup();
}