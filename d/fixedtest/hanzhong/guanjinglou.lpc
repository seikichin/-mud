// guanjinglou —— 由 nitan/d/hanzhong/guanjinglou.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-12230,750,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "观景楼");
  set("long", " ");
  set("exits", ([
    "north" : __DIR__ "houhuayuan",
  ]));
  set("no_fly", 1);
  setup();
}