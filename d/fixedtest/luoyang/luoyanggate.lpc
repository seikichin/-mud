// luoyang 入口房：负图寺·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "负图寺" NOR);
  set("long", "负图寺。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}