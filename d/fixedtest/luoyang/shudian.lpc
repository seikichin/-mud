// shudian —— 由 nitan/d/luoyang/shudian.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-7040,2190,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "老僧书店");
  set("long", @LONG
这里的老板刚从南阳搬来，听说以前他曾在嵩山少林寺出过家，后不
知什么原因又还了俗，专门卖书为生。只见书店面积很大，以前曾是一座
勾栏。
LONG );
  set("exits", ([
    "south" : __DIR__ "wroad7",
  ]));
  set("no_fly", 1);
  setup();
}