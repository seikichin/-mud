// guandaoe4 —— 由 nitan/d/luoyang/guandaoe4.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "大官道");
  set("long", @LONG
这是一条宽阔笔直，尘土飞扬的大官道，足可容得下十马并驰。往东
通向函谷关可以达扬州城，往西则可以到达东都洛阳古城。
LONG );
  set("exits", ([
    "east" : "/d/fixedtest/changan/15/4/0",
    "west" : __DIR__ "guandaoe3",
  ]));
  set("no_fly", 1);
  setup();
}