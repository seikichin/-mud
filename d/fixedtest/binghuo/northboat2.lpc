// northboat2 —— 由 nitan/d/binghuo/northboat2.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "帆船");
  set("long", @LONG
这是一艘三桅海船，不知船主是谁。船上前后两桅挂了白帆，准
备出海。船中舱中备有粮水，足够支用半年。船上水手个个都是哑巴。
主桅上白帆飘动，猎猎作响。
LONG );
  set("no_fly", 1);
  setup();
}