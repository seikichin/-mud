// northboat1 —— 由 nitan/d/binghuo/northboat1.c 转换（独立房间，不在固定地图网格内）
// 原 coor(110,30,20)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "帆船");
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "10/2/1",
  ]));
  set("no_fly", 1);
  setup();
}