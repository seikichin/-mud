// eastboat1 —— 由 nitan/d/binghuo/eastboat1.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "东海海面");
  set("long", @LONG
东海海面波平如镜，大船在海鸥游鱼中穿行，风带风帆，你也不
知道船往哪里去。
LONG );
  set("no_fly", 1);
  setup();
}