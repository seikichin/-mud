// binghuo 出口房：大树林·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "大树林" NOR);
  set("long", "大树林。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}