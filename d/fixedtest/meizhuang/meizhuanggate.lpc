// meizhuang 入口房：甬道·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "甬道" NOR);
  set("long", "甬道。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}