// mishi2 —— 由 nitan/d/meizhuang/mishi2.c 转换（独立房间，不在固定地图网格内）
// 原 coor(30,30,20)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "柳园");
  set("long", " ");
  set("exits", ([
    "west" : __DIR__ "1/2/2",
  ]));
  set("no_fly", 1);
  setup();
}