// mobei 出口房：荒路·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "荒路" NOR);
  set("long", "荒路。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}