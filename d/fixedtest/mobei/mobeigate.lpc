// mobei 入口房：亲卫营·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "亲卫营" NOR);
  set("long", "亲卫营。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}