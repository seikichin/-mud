// foshan 入口房：林间道·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "林间道" NOR);
  set("long", "林间道。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}