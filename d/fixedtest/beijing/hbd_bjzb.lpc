// hbd_bjzb —— 由 nitan/d/beijing/hbd_bjzb.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIB"黑白道总坛"NOR);
  set("long", " ");
  set("exits", ([
    "out" : __DIR__ "7/4/0",
  ]));
  set("no_fly", 1);
  setup();
}