// chaoyangmen —— 由 nitan/d/beijing/chaoyangmen.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-2800,7610,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "朝阳门");
  set("long", @LONG
这里就是京城的朝阳门了，城墙上刻着『朝阳门』三个大字。向
西可以通往京城最热闹王府井大街。这里人来车往，是进出京城的要
道，城门两旁站满了官兵，盘查十分严密。一条宽阔的大道沿着东西
走向延伸，从这里出城到达京城的东郊。
LONG );
  set("exits", ([
    "south" : __DIR__ "matou",
    "east" : __DIR__ "road1",
    "west" : __DIR__ "9/7/0",
  ]));
  set("no_fly", 1);
  setup();
}