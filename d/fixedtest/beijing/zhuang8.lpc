// zhuang8 —— 由 nitan/d/beijing/zhuang8.c 转换（独立房间，不在固定地图网格内）
// 原 coor(90,4730,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "长廊");
  set("long", @LONG
这是条长长的走廊，四处黑沉沉的。
LONG );
  set("exits", ([
    "south" : __DIR__ "zhuang5",
    "north" : __DIR__ "zhuang9",
  ]));
  set("no_fly", 1);
  setup();
}