// beijing 出口房：西长安广场·出口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "西长安广场" NOR);
  set("long", "西长安广场。往 enter 可离开迷宫。\n");
  set("exits", ([
    "enter" : __DIR__ "exit",
  ]));
  set("no_fly", 1);
  setup();
}