// beijing 入口房：安定门·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "安定门" NOR);
  set("long", "安定门。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}