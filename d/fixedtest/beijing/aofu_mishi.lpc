// aofu_mishi —— 由 nitan/d/beijing/aofu_mishi.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "鳌府密室");
  set("long", @LONG
这里是鳌拜府的密室，但是这里似乎也没有珍藏什么特别
的宝贝。周围黑漆漆的，颇为阴森恐怖。
LONG );
  set("exits", ([
    "up" : __DIR__ "0/17/0",
  ]));
  set("no_fly", 1);
  setup();
}