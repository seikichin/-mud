// business —— 由 nitan/d/beijing/business.c 转换（独立房间，不在固定地图网格内）
// 原 coor(-2850,7690,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "北京城商栈");
  set("long", " ");
  set("exits", ([
    "south" : __DIR__ "majiu",
  ]));
  set("no_fly", 1);
  setup();
}