// hangzhou 入口房：临安码头·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "临安码头" NOR);
  set("long", "临安码头。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}