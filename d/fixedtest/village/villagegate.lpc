// village 入口房：东村口·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "东村口" NOR);
  set("long", "东村口。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}