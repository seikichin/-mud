// temple2 —— 由 nitan/d/village/temple2.c 转换（独立房间，不在固定地图网格内）
// 原 coor(30,110,0)  所属格无任何方向位（值为 0 = 引擎不生成房间）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "小棚子");
  set("long", @LONG
这是一个临时搭起来的小棚子。
LONG );
  set("exits", ([
    "west" : __DIR__ "2/11/0",
  ]));
  set("no_fly", 1);
  setup();
}