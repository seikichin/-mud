// 固定地下城测试 2：入口房

inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "枯井" NOR);
  set("long", @LONG
    一口荒废已久的枯井，井口长满了杂草。井壁上嵌着可用于攀爬的石棱。
LONG
  );

  set("exits", ([
    "down": "/d/fixedtest/fixed2/pub/entry",
  ]));

  set("no_fly", 1);
  setup();
}
