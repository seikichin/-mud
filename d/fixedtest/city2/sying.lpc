// sying —— 由 nitan/d/city2/sying.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "宋军大营"NOR);
  set("long", @LONG
这里就是「大宋」军营了，一面大旗高高树起，上面书着一个斗
大的隶书「宋」字，前面不远就是宋军元帅的帐营了。
LONG );
  set("exits", ([
    "north" : __DIR__ "syuanmen1",
    "south" : __DIR__ "sying1",
  ]));
  set("no_fly", 1);
  setup();
}