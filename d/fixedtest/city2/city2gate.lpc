// city2 入口房：承德殿·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "承德殿" NOR);
  set("long", "承德殿。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}