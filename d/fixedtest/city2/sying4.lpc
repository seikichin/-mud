// sying4 —— 由 nitan/d/city2/sying4.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW"弓兵营"NOR);
  set("long", @LONG
这里就是「大宋」弓兵训练大营，一批批的宋军神箭手从这里训
练产生，让蒙古军胆寒，两军交锋，弓箭的使用往往能出奇制胜，因
此大宋也加强了弓箭手的训练。
LONG );
  set("exits", ([
    "north" : __DIR__ "sying1",
    "south" : __DIR__ "syuanmen2",
  ]));
  set("no_fly", 1);
  setup();
}