// sying6 —— 由 nitan/d/city2/sying6.c 转换（独立房间，不在固定地图网格内）
// 原文件无 coor
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "箭楼");
  set("long", @LONG
这里就是「大宋」军营前箭楼。
LONG );
  set("exits", ([
    "east" : __DIR__ "syuanmen1",
  ]));
  set("no_fly", 1);
  setup();
}