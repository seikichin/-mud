// ruzhou 入口房：花亭·入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIW "花亭" NOR);
  set("long", "花亭。往 up 可进入迷宫。\n");
  set("exits", ([
    "up" : __DIR__ "entry",
  ]));
  set("no_fly", 1);
  setup();
}