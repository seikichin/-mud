// huanghe5 —— 由 nitan/d/huanghe/huanghe5.c 转换（独立房间，不在固定地图网格内）
// 原 coor(150,530,0)  超出引擎地图边长上限 100（只保留最密集窗口）
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "黄河岸边");
  set("long", @LONG
这里是黄河岸边。黄河流淌到这里，河床宽阔。黄色的河水在静静
地向东流去，使人忘记它发洪水时的狂威。
LONG );
  set("exits", ([
    "southwest" : __DIR__ "huanghe4",
    "northeast" : __DIR__ "huanghe6",
    "east" : "/d/fixedtest/taishan/5/6/0",
  ]));
  set("no_fly", 1);
  setup();
}